
config changes:

  * We need a working directory to add to every command. Some commands run from
    different directories

  * We need to prompt the user for the args (Env, Password)

    * We need a flag whether to report to slack or not

    * Need slack Webhook in order for the command to work