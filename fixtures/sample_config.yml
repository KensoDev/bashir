---
command_args:
  - environment
  - ansible_pass_file

commands:
  - name: Migrate service X
    command: ansible-playbook ... {.env}
    virtualenv: ansible-deploy
    report_to:
      - KensoDev

  - name: Migrate service X
    command: ansible-playbook ... {.Env}
    virtualenv: ansible-deploy
    report_to:
      - KensoDev
